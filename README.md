# Personal Portfolio Site

A personal portfolio website built with Next.js App Router, TypeScript, Tailwind CSS, and static export.

## Quick Start

```bash
npm install
npm run dev
```

## Build Static Files

```bash
npm run build
```

The exported files will be generated in `out/`.
